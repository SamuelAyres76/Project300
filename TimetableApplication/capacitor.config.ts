import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.timetable.app', // Will need to change this when we get a proper domain
  appName: 'timetable-application',
  webDir: 'dist/timetable-application/browser'
};

export default config;
